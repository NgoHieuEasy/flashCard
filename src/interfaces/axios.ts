// ---------------------------REQUEST---------------------------------

export const netWorkConfig = {
  refetchOnWindowFocus: false,
};
export interface IParamsRequest {
  limit: number;
  page: number;
}
